<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 갤러리-->
    <!-- Android 13 이상 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Android 13 미만 저장-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <!--위치 권한-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hamkkae">

        <activity android:name=".presentation.auth.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.auth.find.FindActivity"
            android:exported="false"/>

        <activity
            android:name=".presentation.main.home.MainActivity"
            android:exported="false">
           <!-- <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>

        <activity android:name=".presentation.main.plan.PlanActivity"
            android:exported="false"/>

        <activity android:name=".presentation.main.trip_continue.TripDetailActivity"
            android:exported="false"/>

        <activity android:name=".presentation.main.upload_photo.UploadPhotoActivity"
            android:exported="false"/>

        <activity android:name=".presentation.main.upload_photo.UploadPhotoCompleteActivity"
            android:exported="false"/>

        <activity android:name=".presentation.main.photo_album.PhotoAlbumActivity"
            android:exported="false"/>
        <activity android:name=".presentation.main.photo_album.detail.PhotoAlbumDetailActivity"
            android:exported="false"/>
        <activity android:name=".presentation.main.trip_records.TripRecordsActivity"
            android:exported="false"/>
        <activity android:name=".presentation.main.trip_records.detail.TripRecordsDetailActivity"
            android:exported="false"/>
        <activity android:name=".presentation.main.map.MapActivity"
            android:exported="false"/>
        <activity android:name=".presentation.main.setting.SettingActivity"
            android:exported="false"/>
    </application>

</manifest>